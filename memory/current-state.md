# Current state

Narrative of the last session or two, plus pointers to where real status lives. Not a queue.

Core rewrites this at session close. Leave it empty until your first close.

- **Last session:** —
- **Pick up next:** —

Authoritative status lives elsewhere: `memory/projects/<project>.md` for project state,
`memory/decisions-log.md` for what was decided, `sessions/YYYY-MM-DD.md` for the day's record.
