# System rundown

End-to-end map of Core's active mechanisms in your install. Populate as you wire things on. Keep current — outdated entries here are worse than missing.

## Engine

Hooks, agents, rules, commands, skills shipped from the template.

## Memory

`memory/` — what lives where, freshness conventions, what's gitignored.

## Sessions

`sessions/YYYY-MM-DD.md` — daily logs.

## Brain (optional)

Vector RAG + knowledge graph at `$CORE_BRAIN`. See `template/brain/README.md` and `scheduling/` for the pipeline.

## External integrations

MCP servers, daemons, scheduled jobs, shell scripts.
